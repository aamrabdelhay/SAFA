(() => {
  const endpoint = '/api/admin/categories';
  const token = () => localStorage.getItem('safaAdminToken') || '';
  const headers = () => ({ 'Content-Type': 'application/json', ...(token() ? { Authorization: `Bearer ${token()}` } : {}) });
  const get = async () => { const r = await fetch(endpoint, { headers: headers() }); const x = await r.json().catch(() => []); if (!r.ok) throw Error(x.error || 'Categories unavailable'); return x; };
  const save = async (method, body) => { const r = await fetch(endpoint, { method, headers: headers(), body: JSON.stringify(body) }); const x = await r.json().catch(() => ({})); if (!r.ok) throw Error(x.error || 'Category operation failed'); return x; };
  const css = () => { if (document.getElementById('safa-category-admin-css')) return; const s = document.createElement('style'); s.id = 'safa-category-admin-css'; s.textContent = `.safa-cat-modal{position:fixed;inset:0;z-index:99999;background:rgba(16,28,44,.55);display:flex;align-items:center;justify-content:center;padding:20px}.safa-cat-panel{background:#fff;max-width:900px;width:100%;max-height:90vh;overflow:auto;padding:28px;border-radius:12px;box-shadow:0 20px 70px rgba(0,0,0,.22)}.safa-cat-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.safa-cat-grid label{display:flex;flex-direction:column;gap:6px;font-size:12px;text-transform:uppercase;letter-spacing:.08em}.safa-cat-grid input,.safa-cat-grid textarea{padding:11px;border:1px solid #ddd;border-radius:6px}.safa-cat-actions{display:flex;gap:8px;justify-content:flex-end;margin-top:16px}.safa-cat-list{margin-top:22px;border-top:1px solid #eee}.safa-cat-row{display:grid;grid-template-columns:1fr 110px 90px 90px;gap:10px;align-items:center;padding:12px 0;border-bottom:1px solid #eee}.safa-cat-row button{padding:7px 9px}.safa-cat-error{color:#9b1c1c;margin-top:10px}@media(max-width:700px){.safa-cat-grid{grid-template-columns:1fr}.safa-cat-row{grid-template-columns:1fr 70px 70px 70px}}`; document.head.appendChild(s); };
  const open = async () => {
    css();
    const modal = document.createElement('div'); modal.className='safa-cat-modal';
    const panel = document.createElement('div'); panel.className='safa-cat-panel'; modal.appendChild(panel); document.body.appendChild(modal);
    let editing = null;
    const render = async () => {
      const cats = await get();
      panel.innerHTML = `<div style="display:flex;justify-content:space-between;align-items:center;gap:12px"><div><small style="letter-spacing:.12em">SAFA / CATALOGUE</small><h2 style="margin:6px 0">Manage Categories</h2></div><button data-close>×</button></div><form data-form><div class="safa-cat-grid"><label>English name<input name="nameEn" required></label><label>Arabic name<input name="nameAr" placeholder="Optional — English name will be used if left empty"></label><label>Slug<input name="slug" placeholder="auto-generated"></label><label>Position<input name="position" type="number" value="0"></label><label>Image URL<input name="imageUrl" placeholder="optional"></label><label>Active<select name="active"><option value="true">Active</option><option value="false">Hidden</option></select></label><label style="grid-column:1/-1">English description<textarea name="descriptionEn"></textarea></label><label style="grid-column:1/-1">Arabic description<textarea name="descriptionAr"></textarea></label></div><div class="safa-cat-actions"><button type="button" data-cancel>Clear</button><button type="submit">${editing?'SAVE CHANGES':'ADD CATEGORY'}</button></div><div class="safa-cat-error" data-error></div></form><div class="safa-cat-list">${cats.map(c=>`<div class="safa-cat-row"><div><b>${escapeHtml(c.name_en)}</b><div style="font-size:12px;opacity:.65">${escapeHtml(c.name_ar)} · ${escapeHtml(c.slug)}</div></div><span>${c.active?'Active':'Hidden'}</span><button data-edit="${c.id}">Edit</button><button data-delete="${c.id}">Delete</button></div>`).join('') || '<p>No categories yet.</p>'}</div>`;
      const form = panel.querySelector('[data-form]');
      if (editing) { const c=cats.find(x=>x.id===editing); if(c){ for(const k of ['nameEn','nameAr','slug','position','imageUrl','descriptionEn','descriptionAr']) if(form.elements[k]) form.elements[k].value=c[k==='nameEn'?'name_en':k==='nameAr'?'name_ar':k==='imageUrl'?'image_url':k==='descriptionEn'?'description_en':k==='descriptionAr'?'description_ar':k] ?? ''; form.elements.active.value=String(c.active); } }
      form.onsubmit = async e => { e.preventDefault(); const fd=new FormData(form); const nameEn=String(fd.get('nameEn')||'').trim(); const body={nameEn,nameAr:String(fd.get('nameAr')||'').trim()||nameEn,slug:fd.get('slug'),position:Number(fd.get('position')||0),imageUrl:fd.get('imageUrl'),descriptionEn:fd.get('descriptionEn'),descriptionAr:fd.get('descriptionAr'),active:fd.get('active')==='true'}; try { await save(editing?'PATCH':'POST', editing?{id:editing,...body}:body); editing=null; await render(); } catch(err){ panel.querySelector('[data-error]').textContent=err.message; } };
      panel.querySelector('[data-cancel]').onclick=()=>{editing=null;render()};
      panel.querySelector('[data-close]').onclick=()=>modal.remove();
      panel.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>{editing=b.dataset.edit;render()});
      panel.querySelectorAll('[data-delete]').forEach(b=>b.onclick=async()=>{if(!confirm('Delete this category? Products will become Uncategorized.'))return;try{await save('DELETE',{id:b.dataset.delete});if(editing===b.dataset.delete)editing=null;await render()}catch(err){panel.querySelector('[data-error]').textContent=err.message}});
    };
    await render();
  };
  const escapeHtml = value => String(value??'').replace(/[&<>'"]/g, ch => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[ch]));
  const inject = () => { if(!localStorage.getItem('safaAdminToken')) return; const nav=document.querySelector('.adminnav'); if(!nav||nav.querySelector('[data-safa-categories]')) return; const b=document.createElement('button'); b.textContent='Categories'; b.dataset.safaCategories='1'; b.onclick=open; nav.appendChild(b); };
  new MutationObserver(inject).observe(document.documentElement,{subtree:true,childList:true}); setInterval(inject,1000); inject();
})();
