(function(){
  const nativeFetch=window.fetch.bind(window);
  const esc=v=>String(v??'').replace(/[&<>"']/g,s=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[s]));
  async function archive(type,id){const r=await nativeFetch('/api/admin-archive',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'archive',type,id})});if(!r.ok){const x=await r.json().catch(()=>({}));throw Error(x.error||'Archive failed')}return r}
  async function permanentDelete(type,id,item){if(!confirm('Are you sure you want to permanently delete this? This cannot be undone'))return;const b=item?.querySelector('.archive-permanent-delete');if(b)b.disabled=true;try{const r=await nativeFetch('/api/admin-archive',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'delete_permanently',type,id})});const x=await r.json().catch(()=>({}));if(!r.ok)throw Error(x.error||'Permanent deletion failed');item?.remove()}catch(e){alert(e.message||'Permanent deletion failed');if(b)b.disabled=false}}
  async function restore(type,id){const r=await nativeFetch('/api/admin-archive',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'restore',type,id})});if(!r.ok){const x=await r.json().catch(()=>({}));throw Error(x.error||'Restore failed')}return r}
  async function loadArchive(){const panel=document.querySelector('.archive-panel');if(!panel)return;const content=panel.querySelector('.archive-content');if(content)content.innerHTML='<p class="archive-empty">Loading archive…</p>';try{const r=await nativeFetch('/api/admin-archive?type=all',{credentials:'same-origin',cache:'no-store'});const data=await r.json();if(!r.ok)throw Error(data.error||'Archive unavailable');const render=(items,type,label)=>`<section class="archive-section"><h3>${label}</h3>${items.length?items.map(x=>{const title=type==='order'?`#${esc(x.order_number)}`:esc(x.name_en),meta=type==='order'?`${esc(x.customer_name)} · ${esc(x.status)}`:(type==='product'?`${esc(x.category_en||'Uncategorized')} · ${esc(x.stock)} in stock`:`${esc(x.slug||'')}`);return `<div class="archive-item"><div class="archive-meta"><strong>${title}</strong><small>${meta}</small></div><div class="archive-actions"><button class="archive-restore" data-restore-type="${type}" data-restore-id="${esc(x.id)}">RESTORE</button>${type==='product'||type==='category'?`<button class="archive-permanent-delete" type="button" data-delete-type="${type}" data-delete-id="${esc(x.id)}">DELETE PERMANENTLY</button>`:''}</div></div>`}).join(''):'<p class="archive-empty">Archive is empty.</p>'}</section>`;content.innerHTML=`<div class="archive-columns">${render(data.orders||[],'order','Orders')}${render(data.products||[],'product','Products')}${render(data.categories||[],'category','Categories')}</div>`}catch(e){content.innerHTML=`<p class="error">${esc(e.message)}</p>`}}
  function ensureArchiveUI(){const nav=document.querySelector('.adminnav'),root=document.querySelector('.admin-clean');if(!nav||!root)return;let btn=nav.querySelector('[data-safa-archive-nav]');if(!btn){btn=document.createElement('button');btn.type='button';btn.textContent='Archive';btn.className='archive-nav-button';btn.dataset.safaArchiveNav='1';nav.appendChild(btn);btn.addEventListener('click',()=>{root.classList.add('archive-open');ensurePanel();loadArchive()})}nav.querySelectorAll('button:not([data-safa-archive-nav])').forEach(x=>{if(x.dataset.safaArchiveBound==='1')return;x.dataset.safaArchiveBound='1';x.addEventListener('click',()=>root.classList.remove('archive-open'))});root.querySelectorAll('.delete-btn').forEach(x=>{if(!x.dataset.safaArchiveLabel){x.dataset.safaArchiveLabel='1';x.textContent='ARCHIVE';x.title='Move to archive'}});document.querySelectorAll('.order-card').forEach(card=>{if(card.querySelector('.order-archive-button'))return;const orderNumber=(card.querySelector('.order-card-head b')?.textContent||'').replace(/^#/,'').trim(),order=Array.isArray(window.__safaOrders)?window.__safaOrders.find(x=>String(x.order_number)===orderNumber):null;if(!order?.id)return;const button=document.createElement('button');button.type='button';button.className='order-archive-button';button.textContent='ARCHIVE';button.addEventListener('click',async()=>{if(!confirm('Move this order to the archive?'))return;try{await archive('order',order.id);card.remove();window.__safaOrders=(window.__safaOrders||[]).filter(x=>x.id!==order.id)}catch(e){alert(e.message)}});card.querySelector('.order-card-head')?.appendChild(button)})}
  function ensurePanel(){const root=document.querySelector('.admin-clean');if(!root||!root.querySelector('.adminnav'))return null;let panel=root.querySelector('.archive-panel');if(panel)return panel;panel=document.createElement('section');panel.className='archive-panel';panel.innerHTML='<div class="archive-toolbar"><div><p class="eyebrow">SAFA / RECOVERY</p><h2>Archive</h2></div><button type="button" class="archive-refresh">REFRESH</button></div><div class="archive-content"></div>';panel.querySelector('.archive-refresh').addEventListener('click',loadArchive);root.appendChild(panel);panel.addEventListener('click',async e=>{const deleteButton=e.target.closest('[data-delete-type]');if(deleteButton){deleteButton.disabled=true;await permanentDelete(deleteButton.dataset.deleteType,deleteButton.dataset.deleteId,deleteButton.closest('.archive-item'));return}const b=e.target.closest('[data-restore-type]');if(!b)return;b.disabled=true;try{await restore(b.dataset.restoreType,b.dataset.restoreId);await loadArchive()}catch(err){alert(err.message);b.disabled=false}});return panel}
  const observer=new MutationObserver(()=>{ensureArchiveUI();if(document.querySelector('.adminnav'))ensurePanel()});observer.observe(document.documentElement,{childList:true,subtree:true});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>{ensureArchiveUI();ensurePanel()},{once:true});else{ensureArchiveUI();ensurePanel()}
})();