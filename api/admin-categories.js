const { Pool } = require('pg');
const jwt = require('jsonwebtoken');

if (!process.env.SESSION_SECRET) throw new Error('SESSION_SECRET is required');
const pool = new Pool({ connectionString: process.env.DATABASE_URL, ssl: process.env.NODE_ENV === 'production' ? { rejectUnauthorized: false } : false, max: 5, idleTimeoutMillis: 10000, connectionTimeoutMillis: 10000 });

function readCookie(req,name){const raw=req.headers.cookie||'';for(const part of raw.split(';')){const [k,...rest]=part.trim().split('=');if(k===name)return decodeURIComponent(rest.join('='));}return ''}
function auth(req,res){try{const bearer=(req.headers.authorization||'').replace(/^Bearer\s+/,'');const token=bearer||readCookie(req,'safa_admin_session');if(!token)throw Error();jwt.verify(token,process.env.SESSION_SECRET);return true}catch{res.status(401).json({error:'Authentication required'});return false}}
const slugify=value=>String(value||'').toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'')||`category-${Date.now()}`;
const clean=value=>String(value??'').trim();
module.exports=async(req,res)=>{if(!auth(req,res))return;const method=req.method.toUpperCase();try{if(method==='GET')return res.json((await pool.query('select * from categories order by position,name_en')).rows);if(method==='POST'){const body=req.body||{},nameEn=clean(body.nameEn),nameAr=clean(body.nameAr)||nameEn;if(nameEn.length<2)return res.status(400).json({error:'English category name is required'});const base=slugify(body.slug||nameEn);let slug=base;for(let i=2;(await pool.query('select 1 from categories where slug=$1',[slug])).rows[0];i++)slug=`${base}-${i}`;const row=(await pool.query('insert into categories(name_en,name_ar,slug,description_en,description_ar,image_url,position,active) values($1,$2,$3,$4,$5,$6,$7,$8) returning *',[nameEn,nameAr,slug,clean(body.descriptionEn),clean(body.descriptionAr),clean(body.imageUrl)||null,Number(body.position||0),body.active!==false])).rows[0];return res.status(201).json(row)}if(method==='PATCH'){const body=req.body||{};if(!body.id)return res.status(400).json({error:'Category id is required'});const fields=[],values=[],map={nameEn:'name_en',nameAr:'name_ar',slug:'slug',descriptionEn:'description_en',descriptionAr:'description_ar',imageUrl:'image_url',position:'position',active:'active'};for(const[key,column]of Object.entries(map)){if(!(key in body))continue;fields.push(`${column}=$${values.length+1}`);let value=body[key];if(key==='nameEn'||key==='nameAr')value=clean(value);if(key==='position')value=Number(value||0);if(key==='imageUrl')value=clean(value)||null;if(key==='slug')value=slugify(value);values.push(value)}if(!fields.length)return res.status(400).json({error:'No changes'});values.push(body.id);const row=(await pool.query(`update categories set ${fields.join(',')},updated_at=now() where id=$${values.length} returning *`,values)).rows[0];if(!row)return res.status(404).json({error:'Category not found'});return res.json(row)}if(method==='DELETE'){const id=req.body?.id||req.query?.id;if(!id)return res.status(400).json({error:'Category id is required'});const client=await pool.connect();try{await client.query('begin');await client.query('update products set category_id=null,updated_at=now() where category_id=$1',[id]);const deleted=(await client.query('delete from categories where id=$1 returning id',[id])).rows[0];if(!deleted){await client.query('rollback');return res.status(404).json({error:'Category not found'})}await client.query('commit');return res.status(204).end()}catch(error){await client.query('rollback');throw error}finally{client.release()}}res.setHeader('Allow','GET,POST,PATCH,DELETE');return res.status(405).json({error:'Method not allowed'})}catch(error){console.error('SAFA category API error:',error?.message||error);return res.status(400).json({error:error?.code==='23505'?'Category slug already exists':(error?.message||'Category operation failed')})}};
