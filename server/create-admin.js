require('dotenv').config();const{Pool}=require('pg'),bcrypt=require('bcryptjs');const p=new Pool({connectionString:process.env.DATABASE_URL});const email=process.argv[2]||'admin@safa.local',password=process.argv[3]||'oa';bcrypt.hash(password,12).then(h=>p.query('insert into admins(email,password_hash) values($1,$2) on conflict(email) do update set password_hash=excluded.password_hash',[email,h])).then(()=>console.log('Admin created:',email)).catch(console.error).finally(()=>p.end());
