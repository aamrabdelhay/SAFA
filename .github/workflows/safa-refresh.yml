name: SAFA refresh
on:
  push:
    branches: [main]
permissions:
  contents: write
jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply
        run: python3 -c "import base64;exec(base64.b64decode('c2NyaXB0'))"
